import math

def area(base, height):
    #Finds area of triangle
    return base * height * 0.5

def perimeter(length1, length2, base):
    #Finds perimeter of triangle
    return (length1 + length2 + base)
